const BASE_BITMASK = (1 << 3);
const BIRD_BITMASK = (1 << 4);
const POINT_BITMASK = (1 << 5);
const DESTROY_BITMASK = (1 << 6);

export {
    BIRD_BITMASK,
    BASE_BITMASK,
    POINT_BITMASK,
    DESTROY_BITMASK
}